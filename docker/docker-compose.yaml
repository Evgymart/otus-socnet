version: "3.9"

networks:
  app-network:
    driver: bridge

services:
  app:
    container_name: app
    build:
      dockerfile: Dockerfile
      context: ./go
      target: dev
    ports:
      - "8080:80"
    volumes:
      - ../src:/opt/app/src
    networks:
      - app-network

